# Daily Log — 2026-05-03
## Better Than Yesterday ✓

### Yesterday's Recap
- Commits: none — no functional work shipped (day 7 of zero commits beyond daily logs)
- Key changes: none. All four carry-over tasks remain untouched and live on the production site.

### Open Items & Blockers
- **[CARRY-OVER x7] No Open Graph / social preview tags on any page** — every outbound link renders a blank card. Active liability on every cold email and LinkedIn share sent since launch.
- **[CARRY-OVER x7] No analytics** — traffic, referral sources, and Calendly conversion rate remain completely invisible. No data is being collected. Every day without Plausible is a day of permanent data loss.
- **[CARRY-OVER x7] index.html service card CTAs broken** — buttons still point to `#` or generic `services.html` instead of `services.html#pricing`. Visitors who click a service card hit a dead end. This has been live since launch.
- **[CARRY-OVER x5] Netlify Forms → CRM/Slack not wired** — contact form submissions arrive silently in the Netlify dashboard with no notification. Leads are being missed.

### Today's Top 3 Focus
1. **[DO IT NOW — 5 minutes] Fix the service card CTAs in index.html.** Open `index.html`. Search for `href="#"` and `href="services.html"` inside the Services Overview section (the 6 service cards). Change every one to `href="services.html#pricing"`. Save. Run: `git add index.html && git commit -m "fix: service card CTAs point to services pricing section" && git push origin main`. This task has been fully specified for 7 days. There is nothing to plan. Open the file.
2. **[30 minutes] Add Open Graph + canonical tags to all 4 pages.** In the `<head>` of each file (`index.html`, `services.html`, `about.html`, `contact.html`) add: `og:title`, `og:description`, `og:image` (use `/og-image.png` at 1200×630), `og:url`, `twitter:card summary_large_image`, and `<link rel="canonical">`. Commit: `feat: add Open Graph and canonical tags to all pages`. This turns every link share into a branded impression — forever, from today forward.
3. **[10 minutes] Add Plausible analytics snippet to all 4 pages.** One line per `<head>`: `<script defer data-domain="leftclick-agency.netlify.app" src="https://plausible.io/js/script.js"></script>`. Register at plausible.io first if needed (2 minutes). Commit: `feat: add Plausible analytics to all pages`. Every day this is delayed is a day of traffic data permanently lost.

### System Improvement Suggestion
> **The log is not the work.** Seven daily logs have been written documenting the same four tasks. The gap is not information — all three tasks above are fully specified with exact file names, exact code, and exact commit messages. The gap is execution. Today's improvement: before writing anything else, open `index.html`, make the CTA fix, and push the commit. One shipped commit today is worth more than eight daily logs. The log exists to track momentum, not substitute for it.

### Sales & Marketing Momentum
- The site is live and being shared in cold outreach with blank social preview cards. Fix OG tags today and every future email link immediately becomes a branded visual impression.
- The CTA fix (task 1) closes a broken conversion path that has been live since launch. A prospect who clicked a service card and hit `#` has already left — do not let the next one.
- Once Plausible is live, set up an outbound click goal for `calendly.com/leftclick-meeting-30` immediately. That gives a real booking conversion rate from day one of data collection.
- **Do not add new pages, new copy, or new sections until tasks 1–3 are committed and pushed.**

### Shipped Today
- [ ] index.html service card CTAs → `services.html#pricing`
- [ ] Open Graph + canonical tags on all 4 pages
- [ ] Plausible analytics snippet on all 4 pages
- [ ] Netlify Forms → Slack/CRM notification wired
