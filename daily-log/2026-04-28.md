# Daily Log — 2026-04-28
## Better Than Yesterday ✓

### Yesterday's Recap
- Commits: `71eeeca chore: daily log 2026-04-27` (log only — no feature work)
- Key changes: No functional changes shipped yesterday. Site is live, all four pages are complete and stable on main.
- Carry-over context: Netlify Forms (`data-netlify="true"`) was already wired on the contact form. All four pages already have `<meta name="description">` tags. The `services.html` pricing section has `id="pricing"`. None of the planned items from yesterday (Open Graph tags, analytics, pricing deep-links from index.html) were shipped.

### Open Items & Blockers
- **No Open Graph / social tags on any page** — sharing the site URL on LinkedIn, Twitter, or Slack shows no preview image or title. For an agency actively doing outbound, this is a credibility gap every time someone pastes the link in a DM.
- **No analytics** — zero visibility into traffic, source, and Calendly conversion rate. Cannot measure what's working.
- **index.html service cards have no link to services.html#pricing** — visitors who want pricing have no direct path from the homepage (anchor exists in services.html but is unreachable from the main CTA flow).
- **Netlify Forms → CRM pipeline not set up** — form submissions go into Netlify's dashboard but are not routed anywhere actionable (CRM, Notion, Slack).

### Today's Top 3 Focus
1. **Add Open Graph + canonical tags to all 4 pages** — Each page needs `og:title`, `og:description`, `og:image`, `og:url`, and `<link rel="canonical">`. Add a 1200×630px OG image (even a simple branded placeholder) to the repo and reference it. This unblocks every outbound link share and is a 30-minute job with compounding payoff.
2. **Add Plausible or GA4 analytics snippet to all 4 pages** — One `<script>` tag in each `<head>`. Plausible is simpler, privacy-friendly, and has no cookie banner requirement. Without this you are flying blind on which page or CTA drives Calendly bookings.
3. **Wire index.html service cards to services.html#pricing** — The 6 service cards on the homepage have CTA buttons. Change their `href` from `#` or `services.html` to `services.html#pricing` so visitors land directly at the pricing tier that matches their interest. This is a 5-minute change that directly reduces friction in the conversion path.

### System Improvement Suggestion
> **Set up a Netlify → Slack (or email) form notification today.** In the Netlify dashboard under Forms → Notifications, add an email or Slack webhook so every contact form submission pings you in real time. This costs zero dev time (it's a UI toggle) and means no lead sits unread in a Netlify dashboard tab you forget to check. Once that's working, layer in a Make/n8n webhook to push the same payload into your CRM — exactly the workflow LeftClick sells to clients, now dogfooding it yourself.

### Sales & Marketing Momentum
- Open Graph tags (priority 1) make every outbound link share — cold email, LinkedIn DM, case study PDF — look polished and clickable. Do this before any active outreach today.
- Once Plausible/GA4 is live, set a goal event on the Calendly link click so you can see which page and which CTA drives the most bookings.
- `services.html` pricing deep-link fix (priority 3) is the smallest change with the most direct conversion impact — do it last but don't skip it.
- Longer-term: the three case study cards on `index.html` (SaaS client 4.2× pipeline, E-Commerce 68% cost reduction, Agency 3× revenue) are your strongest social proof. Expanding each into a standalone `/results/[slug].html` page gives you SEO-indexable content and shareable assets for outbound sequences.
